//: [Previous](@previous)

/*:
 ## week2_STEP3 전역변수 없애기
 함수의 반환값을 사용해서 전역변수를 없앴다!
 처음에는 그 딕셔너리에 회차 정보를 저장할 때 지금까지의 회차 숫자를 매개변수로 받게 했다.
 직접 설정하고 싶었기에. 그런데 그 함수의 반환으로 딕셔너리를 두니까 그 밑에 특정 회차를 프린트하는 부분에서 회차 정보 함수를 불러내서 써야하는데 매개변수 때문에 문제가 생기는... 그래서 아예 회차 프린트하는 함수에서 since로 지금까지의 회차 숫자 정보를, time으로 출력하고 싶은 회차 숫자 정보를 받게 해서 해결!
 */

//: [Next](@next)
